<form method="post" action="{{ path('app_certificat_delete', {'id': certificat.idCertificat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ certificat.idCertificat) }}">
    <button class="btn">Delete</button>
</form>
