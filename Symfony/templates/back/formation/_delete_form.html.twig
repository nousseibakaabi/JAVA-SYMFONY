<form method="post" action="{{ path('app_formation_delete', {'id_formation': formationn.id_formation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formationn.id_formation) }}">
    <button class="btn btn-danger">Delete</button>
</form>
