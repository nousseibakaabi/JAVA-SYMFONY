
{% extends 'base-back.html.twig' %}


{% block title %}Level{% endblock %}

{% block body %}
 

 <section class="section-md bg-default decor-text" >
    <div class="container">
        <h4 class="heading-decorated">Level</h4>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ niveau.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ niveau.name }}</td>
            </tr>
            <tr>
                <th>Prerequis</th>
                <td>{{ niveau.prerequis }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ niveau.duree }}</td>
            </tr>
            <tr>
                <th>Nbformation</th>
                <td>{{ niveau.nbformation }}</td>
            </tr>
            <tr>
                <th>Certificat</th>
                <td>{{ niveau.certificat }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ niveau.description }}</td>
            </tr>
            <tr>
                 <th>Image</th>
                 <td>{{ niveau.image}}</td>
                  <td><img src="{{ asset('uploads/images/' ~ niveau.image) }}" height="300" width="300" alt="Img"></td>
                
            </tr>
        </tbody>
    </table>

    

    {{ include('niveau/_delete_form.html.twig') }}
     </div>
</section>
{% endblock %}
