<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reservation</title>
    <style>
        body {
            font-family: Roboto;
        }

        .certificate-container {
            padding: 50px;
            width: 100%; /* Utiliser toute la largeur disponible */
            display: flex; /* Utiliser un layout en ligne */
            justify-content: center; /* Centrer le contenu horizontalement */
        }

        .certificate {
            border: 20px solid #0C5280;
            padding: 25px;
            height: 600px;
            position: relative;
            display: flex; /* Utiliser un layout en ligne */
            flex-direction: column; /* Disposition verticale pour les éléments internes */
            justify-content: space-between; /* Répartir l'espace vertical entre les éléments */
            align-items: center; /* Centrer les éléments horizontalement */
        }

        .certificate:after {
            content: '';
            top: 0px;
            left: 0px;
            bottom: 90px;
            right: 0px;
            position: absolute;
            background-image: url('http://127.0.0.1:8000/back/images/logo.png');
            background-size: cover;
            z-index: -1;
            opacity: 0.15;
        }

        .certificate-header {
            position: relative;
            width: 100%; /* Utiliser toute la largeur disponible */
            display: flex; /* Utiliser un layout en ligne */
            justify-content: space-between; /* Répartir l'espace horizontal entre les éléments */
        }

        .certificate-header > .logo,
        .certificate-header > .logoo {
            width: 110px;
            height: 110px;
        }

        .certificate-title {
            text-align: center;
            font-size: 25px;
            color: #CE4524;
            margin-top: 20px; /* Ajouter un espace supplémentaire au-dessus du titre */
        }

        .certificate-body {
            text-align: center;
        }

        h1 {
            font-weight: 400;
            font-size: 36px;
            color: #0C5280;
            font-family: Maharlika;
            margin-top: 20px; /* Ajouter un espace supplémentaire au-dessus du titre */
        }

        .student-name {
            font-size: 24px;
            font-weight: bold;
        }

        .certificate-content {
            margin: 0 auto;
            width: 750px;
        }

        .about-certificate {
            width: 350px;
            margin: 0 auto;
        }

        .topic-description {
            text-align: center;
            font-size: 18px;
        }

        .text-muted {
            color: #6c757d;
        }

        .certificate-footer {
            margin-top: 20px;
        }

        .btn {
            margin-top: 20px;
        }
    </style>
</head>
<body>
<div class="certificate-container">
    <div class="certificate">
        <div class="water-mark-overlay"></div>



        <div class="certificate-body">
            <p class="certificate-title">{{ reservation.name }}</p>
            <p>Event Name :</p>
            <p class="student-name">{{ reservation.namee }}</p>
            <div class="certificate-content">
                <div class="about-certificate">
                    <p>Reserved Places : <strong>{{ reservation.nbPlaces }}</strong>with purchase of <strong>{{ paiement.montant }}</strong>.</p>
                </div>
                <p class="topic-description">Reserved AT  {{ paiement.heureP | date('Y-m-d H:i:s')}}.</p>
                <div class="text-center">
                    <p class="topic-description text-muted">This Event was issued by <strong>TANIT ONLINE</strong>.</p>
  
            <div class="certificate-footer text-muted">
            </div>
        </div>
    </div>
</div>



</body>
</html>
