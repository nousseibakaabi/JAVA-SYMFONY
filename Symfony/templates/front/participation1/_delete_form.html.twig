<form method="post" action="{{ path('app_participation1_delete', {'id': participation1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participation1.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
