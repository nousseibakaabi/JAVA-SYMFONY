{% extends 'backend/Dashboard.html.twig' %}

{% block title %}Participation1{% endblock %}

{% block body %}
    
<section class="section-md bg-default decor-text" >
    <div class="container">
        <h4 class="heading-decorated">Participation</h4>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participation1.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ participation1.startdate ? participation1.startdate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
           
            <tr>
                <th>Participationstatus</th>
                <td>{{ participation1.participationstatus }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ participation1.comment }}</td>
            </tr>
        </tbody>
    </table>

   
    {{ include('participation1/_delete_form.html.twig') }}
{% endblock %}
