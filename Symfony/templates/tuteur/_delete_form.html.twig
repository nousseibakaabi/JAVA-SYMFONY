<form method="post" action="{{ path('app_tuteur_delete', {'idTuteur': tuteur.idTuteur}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tuteur.idTuteur) }}">
    <button class="btn">Delete</button>
</form>
